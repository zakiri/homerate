# 🏗️ HomeRate Security Architecture

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        HTTP/WebSocket Requests                      │
│                         (From Clients)                               │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────────────┐
        │   DDoS & Attack Prevention Bot Middleware  │
        │  (Real-time IP blocking, Rate limiting)   │
        │  - Check IP Whitelist/Blacklist           │
        │  - Validate Payload (SQL/XSS/Cmd injection│
        │  - Rate Limiting (100 req/min per IP)     │
        └────────┬─────────────────────────────────┘
                 │ ✅ Passed / 🚫 Blocked
                 ▼
        ┌────────────────────────────────────────────┐
        │    Express Route Handlers / Controllers    │
        │     (Auth, Transaction, Portfolio, etc)   │
        └────────┬─────────────────────────────────┘
                 │
                 ▼
        ┌─────────────────────────────────────────────────────┐
        │                                                      │
        │   🔐 SECURITY VALIDATION LAYER                      │
        │                                                      │
        │  ┌──────────────────────────────────────────────┐   │
        │  │   Security Monitoring Service (Coordinator)  │   │
        │  │  - validateTransaction()                     │   │
        │  │  - calculateRiskScore()                      │   │
        │  │  - recordSuspiciousActivity()                │   │
        │  └──────────────────────────────────────────────┘   │
        │                      │                               │
        │     ┌────────────────┼────────────────┐             │
        │     ▼                ▼                ▼             │
        │  ┌─────────┐ ┌──────────┐ ┌──────────┐            │
        │  │ Amount  │ │Frequency │ │ Front-   │            │
        │  │Anomaly  │ │Anomaly   │ │running   │            │
        │  └─────────┘ └──────────┘ └──────────┘            │
        │                                                      │
        └─────────────────────────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
    ┌─────────────┐  ┌─────────────────┐  ┌──────────────┐
    │ Risk Score  │  │ Security Flags  │  │ Block Signal │
    │ 0-100       │  │ (Issues found)  │  │ (if Critical)│
    └─────────────┘  └─────────────────┘  └──────────────┘
         │                   │                   │
         └───────────────────┼───────────────────┘
                             ▼
        ┌────────────────────────────────────────────┐
        │   Transaction Object Updated With:         │
        │  - Risk Score                              │
        │  - Security Flags                          │
        │  - Blocked Status (if score > 80)          │
        └────────┬─────────────────────────────────┘
                 │
         ┌───────┴───────┐
         │               │
         ▼               ▼
    ┌─────────┐     ┌──────────┐
    │ ✅SAVE  │     │ 🚫REJECT │
    │to DB    │     │(403 Error)│
    └─────────┘     └──────────┘
```

---

## Bot Ecosystem

```
                    ┌─────────────────────────────┐
                    │   Server Startup            │
                    │   index.js                  │
                    └────────────┬────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │ Start All Bots          │
                    └─┬──┬──┬──┐             │
                      │  │  │  │             │
        ┌─────────────┘  │  │  └───────────┐ │
        │   ┌────────────┘  │        ┌─────┘ │
        │   │   ┌───────────┘   ┌────┘       │
        │   │   │        ┌──────┘            │
        ▼   ▼   ▼        ▼                   ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │   Anomaly    │ │    Price     │ │    Fraud     │
    │  Detection   │ │Manipulation  │ │  Detection   │
    │     Bot      │ │      Bot     │ │      Bot     │
    │              │ │              │ │              │
    │ Interval:    │ │ Interval:    │ │ Interval:    │
    │ 5 seconds    │ │ 10 seconds   │ │ 15 seconds   │
    │              │ │              │ │              │
    │ Monitored:   │ │ Monitored:   │ │ Monitored:   │
    │ • Amounts    │ │ • Prices     │ │ • Logins     │
    │ • Frequency  │ │ • Volatility │ │ • Devices    │
    │ • Patterns   │ │ • Volume     │ │ • Passwords  │
    │ • Clustering │ │ • Pump/Dump  │ │ • 2FA        │
    │              │ │ • Wash Trade │ │ • Bot Nets   │
    └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
           │                │                │
           └─────────────────┼────────────────┘
                             │
                             ▼ (All feed data to)
                    ┌────────────────────────┐
                    │  DDoS Prevention Bot   │
                    │       (Real-time)      │
                    │                        │
                    │ Functions:             │
                    │ • Check every request  │
                    │ • Rate limiting        │
                    │ • IP blocking          │
                    │ • Payload validation   │
                    │ • Scanner detection    │
                    └────────────────────────┘
                             │
           ┌─────────────────┴────────────────┐
           │                                  │
           ▼                                  ▼
    ┌─────────────────┐            ┌──────────────────┐
    │ Alert Storage   │            │ Blocked IPs      │
    │ (Max 1000)      │            │ (Auto-cleanup)   │
    │                 │            │                  │
    │ • Type          │            │ • Blocking time  │
    │ • Severity      │            │ • Reason         │
    │ • User ID       │            │ • Auto-unblock   │
    │ • Details       │            │                  │
    │ • Timestamp     │            │                  │
    └─────────────────┘            └──────────────────┘
           │
           ▼
   ┌──────────────────────────┐
   │   Security API Routes    │
   │   /api/security/*        │
   │                          │
   │ • Dashboard              │
   │ • Alerts                 │
   │ • Bot Management         │
   │ • IP Management          │
   └──────────────────────────┘
```

---

## Transaction Processing Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                   New Transaction Request                        │
│              POST /api/transaction                              │
└────────────────────────┬─────────────────────────────────────────┘
                         │
            ┌────────────┴────────────┐
            │                         │
            ▼                         ▼
     ┌─────────────┐      ┌──────────────────┐
     │  Extract    │      │ IP, User-Agent   │
     │  Request    │      │ Headers Captured │
     │  Body       │      └──────────────────┘
     └─────┬───────┘
           │
           ▼
    ┌─────────────────────────────────────────┐
    │  Create Transaction Object              │
    │  - userId, walletAddress                │
    │  - type (buy/sell/swap/transfer)        │
    │  - fromSymbol, toSymbol                 │
    │  - fromAmount, toAmount, price          │
    │  - gasUsed, gasFee                      │
    │  - clientIP ← Network info              │
    │  - userAgent ← Browser info             │
    │  - status: pending                      │
    └──────────┬────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────┐
    │ 🔒 SECURITY VALIDATION                   │
    │ await securityMonitoringService          │
    │     .validateTransaction(tx)             │
    └──────────┬────────────┬────────────────────┘
               │            │
         ┌─────▼─────┐      │
         │ 5 Checks: │      │
         │ 1. Amount │      │
         │ 2.Freqncy │      │
         │ 3.Address │      │
         │ 4. Price  │      │
         │ 5. Replay │      │
         └─────┬─────┘      │
              │              │
              ▼              │
     ┌──────────────────┐   │
     │ Calculate        │   │
     │ Risk Score       │   │
     │ (Σ Severities)   │   │
     └────────┬─────────┘   │
              │              │
              └──────┬───────┘
                     │
            ┌────────▼────────┐
            │ Risk Score      │
            │ Decision Point  │
            └────────┬────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        │ ✅ < 80   │ 🟡 60-80   │ 🔴 > 80
        │            │            │
        ▼            ▼            ▼
    ┌────────┐  ┌────────┐  ┌─────────┐
    │ ALLOW  │  │ WARN & │  │ BLOCK   │
    │ SAVE   │  │ FLAG   │  │ REJECT  │
    │        │  │ SAVE   │  │ 403     │
    └────┬───┘  └────┬───┘  └────┬────┘
         │           │           │
         └───────┬───┴───────────┘
                 │
         ┌───────▼──────────┐
         │ Log to Alerts    │
         │ Update Behavior  │
         │ Send Response    │
         └──────────────────┘
```

---

## Real-time Monitoring Chain

```
Every 5 seconds:
    Anomaly Detection Bot
    └─ Fetch last 5 sec transactions
    └─ Check statistical anomalies
    └─ Check behavior anomalies
    └─ Check network anomalies
    └─ If found: Add alert & update profile

Every 10 seconds:
    Price Manipulation Bot
    └─ Fetch last 1 minute transactions
    └─ Analyze price history (100 per pair)
    └─ Check volatility spikes
    └─ Check pump & dump patterns
    └─ Check wash trading
    └─ Check slippage manipulation
    └─ If found: Add alert

Every 15 seconds:
    Fraud Detection Bot
    └─ Fetch last 5 minutes events
    └─ Check transaction speeds
    └─ Check device/IP changes
    └─ Check wallet behavior
    └─ Check account changes
    └─ Check password/2FA status
    └─ If found: Add alert

Continuously (Real-time):
    DDoS Prevention Bot
    └─ On every HTTP request:
        ├─ Check if IP blocked
        ├─ Validate rate limit
        ├─ Check payload injection
        ├─ Update request log
        └─ If suspicious: Block IP

All Bots:
    └─ Write to: securityMonitoringService.alerts[]
    └─ Max 1000 alerts (FIFO)
    └─ Alerts have: type, severity, timestamp, details
```

---

## Data Flow Diagram

```
                        ┌─────────────────┐
                        │   MongoDB       │
                        │   Database      │
                        └────────┬────────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
                    ▼            ▼            ▼
                ┌────────┐  ┌────────┐  ┌────────┐
                │ Users  │  │ Trans- │  │Port-   │
                │        │  │actions │  │folios  │
                ├────────┤  ├────────┤  ├────────┤
                │ 5K+    │  │ 100K+  │  │ 2K+    │
                │ indexed│  │indexed │  │indexed │
                └────────┘  └████████┘  └────────┘
                                 ▲
                                 │ (Every 5-15 sec)
                                 │ (Query last N)
                       ┌─────────┴────────────┐
                       │                      │
                   ┌───▼────┐          ┌──────▼──┐
                   │  Bots  │          │DDoS Bot │
                   │ (3)    │          │(realtime)│
                   └───┬────┘          └──────────┘
                       │
                       ▼
                ┌──────────────┐
                │    Alerts    │
                │  (In Memory) │
                │  Max 1000    │
                └──────┬───────┘
                       │
              ┌────────┴────────┐
              │                 │
              ▼                 ▼
        ┌──────────────┐  ┌───────────┐
        │ /api/security│  │ WebSocket │
        │  Dashboard   │  │(Real-time)│
        └──────────────┘  └───────────┘
```

---

## Severity & Risk Model

```
            CRITICAL (🔴)
              Risk > 80
         Auto-block transaction
    ┌─────────────────────────────┐
    │ • Replay Attack             │
    │ • Double Spending           │
    │ • 2FA Disabled              │
    │ • DDoS Attack               │
    │ • Bot Network               │
    └─────────────────────────────┘
              ▲
              │ (accumulate issues)
              │
            HIGH (🟠)
           Risk 50-80
        Flag for review
    ┌─────────────────────────────┐
    │ • Price Manipulation        │
    │ • Wash Trading              │
    │ • Brute Force               │
    │ • Account Takeover Attempt  │
    │ • Unauthorized Wallet       │
    └─────────────────────────────┘
              ▲
              │
              │
          MEDIUM (🟡)
         Risk 25-50
       Monitor closely
    ┌─────────────────────────────┐
    │ • Frequency Anomaly         │
    │ • New Device                │
    │ • Volume Spike              │
    │ • New Symbol                │
    └─────────────────────────────┘
              ▲
              │
              │
            LOW (⭕)
           Risk < 25
         Log & Continue
    ┌─────────────────────────────┐
    │ • Informational             │
    │ • Trend Analysis            │
    └─────────────────────────────┘
```

---

## Performance Architecture

```
Request Rate: 100-1000 req/sec
Active Users: Up to 10,000
Transactions/min: 100-500
Price Updates/sec: 10-50

┌─────────────────────────────────┐
│    Incoming Requests            │
│    (DDoS Bot: Rate limit 100/m) │
└────────┬─────────────────────────┘
         │
    ┌────▼──────┐
    │ Memory    │ (Alert cache)
    │ Store     │ (1000 max)
    │ O(1)      │
    └────┬──────┘
         │
    ┌────▼──────────────────┐
    │ MongoDB Queries       │
    │ • Indexed lookups     │
    │ • Batch operations    │
    │ • Connection pooling  │
    └─────────────────────────┘

Database Indexes:
- userId, createdAt (compound)
- walletAddress, createdAt
- fromSymbol, toSymbol, createdAt
- status
- createdAt (TTL cleanup)
```

---

## Alert Routing

```
Alert Generated
    │
    ├─ CRITICAL (🔴) → Block transaction immediately
    │                  Log to database
    │                  Send to admin alert queue
    │                  Trigger webhook notif
    │
    ├─ HIGH (🟠) → Mark transaction with flags
    │               Log to alerts
    │               Update user profile risk score
    │               Add to monitoring queue
    │
    ├─ MEDIUM (🟡) → Log to alerts
    │                Update behavior profile
    │                Monitor for patterns
    │
    └─ LOW (⭕) → Log to alerts
                  Statistics only
                  No action required
```

---

**Architecture Version:** 1.0.0
**Last Updated:** 11 Şubat 2026
**Bots Deployed:** 4 (All Active)
**Total Security Layers:** 5
**API Endpoints:** 20+

